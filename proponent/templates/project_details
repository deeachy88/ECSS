<table class="table table-hover table-bordered table-centered mb-0">
    <thead>
        <tr>
            <th>Sl.no.</th>
            <th>Production Capacity(MT)</th>
            <th>Stripping Ratio</th>
            <th>Bench Height</th>
            <th>Bench Width</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for project_details in project_details %}
            <tr>
                <td>{{ forloop.counter }}</td>
                <td>{{project_details.capacity}}</td>
                <td>{{project_details.stripping_ratio}}</td>
                <td>{{project_details.bench_height}}</td>
                <td>{{project_details.bench_width}}</td>
                <td>
                    <div class="btn-group btn-group-sm">
                        <a class="btn btn-outline-primary btn-sm" data-toggle="tooltip" title="Edit" href="populate_project_details('{{project_details.record_id}}','{{project_details.application_no}}','{{project_details.capacity}}','{{project_details.stripping_ratio}}','{{project_details.bench_height}}','{{project_details.bench_width}}')"><i class="mdi mdi-square-edit-outline"></i></a>
                        <button  type="button" class="btn btn-outline-danger btn-sm" data-toggle="tooltip" title="Delete" onclick="populate_delete_products_by_products('{{project_details.record_id}}','{{project_details.application_no}}')"><i class="uil-trash"></i></button>
                    </div>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
